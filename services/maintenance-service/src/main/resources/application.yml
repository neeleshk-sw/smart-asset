server:
  port: 8088

spring:
  application:
    name: maintenance-service
  config:
    import:
      - classpath:application-common.yml
      - classpath:application-services.yml
  datasource:
    username: ${DATABASE_USER:maintenance_user}
    password: ${DATABASE_PASSWORD:maintenance@123}
  jpa:
    properties:
      hibernate:
        default_schema: maintenance_schema
