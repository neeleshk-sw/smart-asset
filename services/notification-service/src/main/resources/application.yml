server:
  port: 8087

spring:
  application:
    name: notification-service
  config:
    import:
      - classpath:application-common.yml
      - classpath:application-services.yml
  datasource:
    username: ${DATABASE_USER:notification_user}
    password: ${DATABASE_PASSWORD:notification@123}
  jpa:
    properties:
      hibernate:
        default_schema: notification_schema
