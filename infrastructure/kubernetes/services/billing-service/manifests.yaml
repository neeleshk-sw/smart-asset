apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: smartasset
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      containers:
        - name: billing-service
          image: smartasset/billing-service:latest
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: smartasset-common-config
            - secretRef:
                name: smartasset-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8089
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8089
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: smartasset
spec:
  type: ClusterIP
  ports:
    - port: 8089
      targetPort: 8089
  selector:
    app: billing-service
